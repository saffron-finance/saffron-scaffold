/** Shared CSS breakpoint; importing it never initializes UI components. */
export const mobileHomeMaxWidth = 599

/** Inclusive tablet range; keep Sidebar.css's first-paint media rule aligned. */
export const tabletSidebarQuery = '(min-width: 600px) and (max-width: 1000px)'

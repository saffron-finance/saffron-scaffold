# Live APR merge 0.2.0 — published 12 September 2026

URL: https://clawbee.xyz/saffron/apps/feature-lab/live-apr-merge/

- Standalone frontend source: `c1d55ca`.
- Combined-repository import: `4c2119a`.
- Backend compatibility tested: `19ad0e9`.
- Published mode: lab preview, with the existing read-only live APR gateway.
- Actual payment mode is a separate `build:live` output, not enabled on this URL.

## Verification

47 unit tests passed. Normal, lab and live builds passed. Both compiled preview
browser suites passed 15 grouped checks. The API-connected frontend completed
two separately paid requests and a full position lifecycle using generated
wallets, disposable PostgreSQL, local protocol contracts and exact fork simulation.
No live funds or existing VNC session state were used or changed.

The source archive contains 263 files. Every archived hash matches the tested
source and the combined-repository subtree. Archive SHA-256:
`82bc8c8910bf8e26fc6d829eba12555286e2403a4740b0b434eb61d50fbc1218`.

All 105 current deployed files match the build/source archive. Old content-hash
assets remain for existing open tabs. The public page, release metadata, guide
and Live APR route return the expected 401 without authentication. Authenticated
public browser interaction was not performed during this release; compiled
interaction was tested on loopback and the deployed bytes were matched exactly.

## Backup and operating boundary

Before publication, all 180 prior files were backed up and checked against a
SHA-256 manifest. A full Git bundle was verified. The nginx include was copied.
Before replacement, the published files were checked again to detect concurrent
changes. Publication installed assets first and atomically replaced index.html.

VPS backup/evidence folder, relative to the Saffron workspace:
`feature-lab/live-apr-merge-work/product-sync-20260912/`.
It contains `before.bundle`, `before-deployed/`, `before-manifest.json`,
`before-nginx.conf`, `deployed-manifest.json`, logs and the isolated build checkout.

The original standalone source checkout was fast-forwarded to `c1d55ca`.
No nginx rule, APR collector/API, database migration, signer configuration, VNC
service or browser storage was changed. Use the frontend deployment runbook to
restore static files; a Git rollback alone does not restore a published release.
